# KRI forecast: {{ params.column }}

**Source:** `{{ params.source }}` (column `{{ params.column }}`) &middot; **Discount factor:** {{ params.discount }}
&middot; **Warm-up:** {{ params.warmup }} periods &middot; **Horizon:** {{ params.horizon }} period(s)
{% if params.threshold is not none %}
&middot; **Threshold:** > {{ params.threshold }}
{% endif %}

## History

<details>
<summary>{{ history | length }} periods</summary>

| Period | Count |
|---|---|
{% for period, count in history %}
| {{ period }} | {{ count }} |
{% endfor %}

</details>

## Forecast for the next {{ params.horizon }} period(s)

| | |
|---|---|
| Effective (discounted) events | {{ "%.2f" | format(forecast.effective_events) }} |
| Effective (discounted) exposure | {{ "%.2f" | format(forecast.effective_exposure) }} periods |
| Posterior mean rate | {{ "%.3f" | format(forecast.rate_mean) }} per period |
| Rate 90% credible interval | {{ "%.3f" | format(forecast.rate_interval_90[0]) }} – {{ "%.3f" | format(forecast.rate_interval_90[1]) }} |
| Predictive mean count | {{ "%.2f" | format(forecast.predictive_mean) }} |
| Predictive 50% interval | {{ forecast.predictive_interval_50[0] }} – {{ forecast.predictive_interval_50[1] }} |
| Predictive 90% interval | {{ forecast.predictive_interval_90[0] }} – {{ forecast.predictive_interval_90[1] }} |
{% if forecast.threshold is not none %}
| P(count > {{ forecast.threshold }}) | {{ forecast.prob_exceed_threshold | pct(1) }} |
{% endif %}

## Trend test

{{ trend.interpretation }}

| | |
|---|---|
| Periods | {{ trend.periods }} |
| Rate ratio per period | {{ "%.3f" | format(trend.rate_ratio_per_period) }} |
| 95% CI | {{ "%.3f" | format(trend.rate_ratio_ci_95[0]) }} – {{ "%.3f" | format(trend.rate_ratio_ci_95[1]) }} |
| Likelihood-ratio statistic | {{ "%.2f" | format(trend.likelihood_ratio_statistic) }} |
| p-value | {{ "%.4f" | format(trend.p_value) }} |

## Backtest (rolling-origin, one-step-ahead)

{{ backtest.periods_evaluated }} periods evaluated after the {{ params.warmup }}-period warm-up.

| Interval | Nominal coverage | Observed coverage |
|---|---|---|
| 50% | 50% | {{ backtest.coverage_50 | pct(1) }} |
| 90% | 90% | {{ backtest.coverage_90 | pct(1) }} |

Mean log predictive score: {{ "%.3f" | format(backtest.mean_log_score) }} (a proper scoring rule; less
negative is better; useful only to compare models on the same data).

<details>
<summary>Per-period backtest detail</summary>

| Period | Observed | Predicted mean | 90% interval | Inside 50% | Inside 90% | Log score |
|---|---|---|---|---|---|---|
{% for rec in backtest.records %}
| {{ rec.period }} | {{ rec.observed }} | {{ "%.2f" | format(rec.predicted_mean) }} | {{ rec.interval_90[0] }} – {{ rec.interval_90[1] }} | {{ "✅" if rec.inside_50 else "❌" }} | {{ "✅" if rec.inside_90 else "❌" }} | {{ "%.2f" | format(rec.log_score) }} |
{% endfor %}

</details>

## How to read this

This is a **probabilistic** forecast, not a point prediction. The count per period is modelled as Poisson
with a Gamma belief about its rate; a discount factor of {{ params.discount }} down-weights older periods by
`discount^age`, so the model adapts to a shifting rate (`1.0` means the rate is assumed stationary). The
predictive interval is wider than a plain Poisson interval because it also carries the *uncertainty about
the rate itself* (a negative-binomial predictive distribution). The 50% and 90% intervals are not
confidence intervals on a fixed number: they are ranges that should contain the actual outcome about 50% and
90% of the time, respectively, if the model is well calibrated. The backtest checks exactly that, by
re-forecasting every period from only the data before it and recording how often the outcome fell inside
each interval ("rolling-origin" evaluation, so no future data leaks into a forecast). Observed coverage
close to the nominal 50%/90% means the model's uncertainty is realistic; observed coverage well below
nominal means the intervals are too narrow (overconfident), and well above means they are too wide
(underconfident). The trend test is a separate question — whether the *rate itself* is moving over time —
and does not by itself change the forecast above unless the discount factor is also adjusted to track it.
